To start container or windows run:

yarn start
docker build . -t ddigby/online-ide
docker run --name online-ide-server -p 8080:8080 -v "//var/run/docker.sock:/var/run/docker.sock" -d ddigby/online-ide